## DevOps_Hands_On_Practice_L1 / week1 / Day06

## Day 06 - DEBUG DAY

Fix these broken files:

## Ansible (broken)

- name Install nginx
  hosts all
  become yes
  tasks:
    - name: Install nginx
      yum:
        name nginx
	state present


## Terrform (broken)

resource "aws_instance" "BrokenEC2" {
   ami = "ami-0xyz"
   instance_type = t2.micro
tags {
   Name = "Broken"
}
}


## Kubernetes (borken)

apiVersion v1
kind pod 
metaata:
  name pathnex
spec
  containers:
      - name app
        image nginx


## Debugging pipelines
## Jenkins pipeline - Debugging
You will learn how to **fix broken stages and handle errors**.


pipeline {
   agent any
   stages {
      stage('Checkout') {
         steps {
	    git branch: 'main', url: 'https://github.com/Pathnex/sample-java-app.git'
	 }
      }
      stage('Build') {
         steps {
	    sh ```
	    mvn clean compile || { echo "Compile failed"; exit 1 ; }
	    ```
	 }
      }
      stage('Test') {
         steps {
	    sh ```
	    mvn test || { echo "Tests failed" ; exit 1 ;}
	    ```
	 }
      }
   }
 }


## GitLab CI - Debugging
You will learn how to **handle job failure and exit codes**.


stages:
  - build
  - test

build:
  stage: build
  image: maven:3.8.1-jdk-17
  script:
     - git clone https://github.com/Pathnex/sample-java-app.git
     - cd sample-java-app
     -mvn clean compline || { echo "Compile failed"; exit 1;}

test:
  stage: test
  image: maven:3.8.1-jdk-17
  script:
     - cd sample-java-app
     - mvn test || { echo "Tests failed"; exit 1;}



## DevOps_hands_On_Practice_L2 / week1 / Day06
## Day 06 - Introduction to CloudFormation and kubernetes Advanced Concepts

## Ansible - Install Docker

```yaml
- name: Install Docker
  hosts: all
  become: yes
  tasks:
    - name: Install Docker
      yum:
        name: docker
	state: present
    - name: Start Docker
      service:
         name: docker
	 state: started
	 enabled: yes


## Terraform - Provisioning EC2 with VPC
resource "aws_instance" "main" {
   cidr_block = "10.0.0/16"
  }

  resource "aws_instance" "pathnex_ec2" {
     ami                = "ami-0abcd1234abcd1234"
     instance_type      = "t3.medium"
     subnet_id          = aws_subnet.main.id
     tags = {
       Name = "Pathnex-EC2"
     }
 }

 ## Kubernetes - Pod with Volume Mount
 apiVersion: v1
 kind: pod
 metadata:
    name: pathnex-pod
 spec:
    containers:
       - name: web
         image: nginx
	 volumeMounts:
	     - mountPath: /usr/share/nginx/html
	       name: html-volum
 volumes:
    - name: html-volume
    hostsPath:
       path: /data
       type: Directory


## Shell Script - Check for Running Services
#!/bin/bash
services=("nginx" "docker" "httpd")
for service in "$(services[@]}"
do
  if systemctl is-active --quiet $service: then
    echo "$service is running"
  else
    echo "$service is not running"
  fi 
done


## Docker 
# Node.js Application (javaScript)
console.log("Hello Pathnex");

# Docker File
FROM node:18
WORKDIR /opt/pathnex/node-app
COPY app.js /opt/pathnex/node-app/
CMD ["node', '/opt/pathnex/node-app/app.js"]

# Real Path 
/opt/pathnex/node-app

       
   



