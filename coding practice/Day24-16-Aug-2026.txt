# Day 24 - Advanced Terraform Modules and CI/CD pipeline Scaling

## Ansible - Deploy Docker Containers with Kubernetes Using Helm

```yaml
- name: Deploy Docker Containers with Hwelm
  hosts: localhost
  tasks:
    - name: Install Helm
      command: curl https://raw.githubusercontent.com/helm/master/scripts/get-helm-3 | bash
    - name: Deploy microservices with helm
      command: helm install pathnex-app ./microservices-chart


## Terraform - Multi-Module Infrastruture with Outputs
module "pathnex_vpc" {
   source = "./vpc"
}

module "pathnex_instance" {
   source = "./instance"
   vpc_id = module.pathnex_vpc.vpc_id
}

output "instance_id" {
   value = module.pathnex_instance.instance_id
}


## Kubernetes -Helm-based Auto-Scaling Deployments
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pathnex-web-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pathnex-web-app
   template:
     metadata:
       labels:
         app: pathnex-web-app
     spec:
      containers:
         - name: nginx
	   image: nginx
	   ports:
	      - containerPort: 80

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscalar
metadata:
  name: pathnex-web-app-hpa
spec:
  scaleTargetRef:
     apiVersion: apps/v1
     kind: Deployment
     name: pathnex-web-app
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80


## Jenkinsfile - Scaling CI/CD Pipeline
pipeline {
  agent any
  stages {
     stage('Scale Up') {
        steps {
	   echo 'Scaling up Jenkins pipeline agents for heavy workloads...'
	   // Simulating scaling logic
	} 
     }
     stage('Build') {
         steps {
	     echo 'Building project...'
	 }
      }
      stage('Deploy') {
          steps {
	      echo 'Deploying projects...'
	  }
      }
   }
}


## Docker 
# Healthcheck
FROM nginx
HEALTHCHECK CMD CURL --fail http://localhost || exit 1

